# GENERATED BY MATUGEN - do not edit by hand.
# Template: ~/.config/matugen/templates/ranger-accent
# Wallpaper: {{image}}
#
# Read by ~/.config/ranger/colorschemes/tokyonight.py when ranger starts.
#
# In DECIMAL RGB, not hex: ranger is curses and only understands indices into
# the 256-colour palette, so the colorscheme has to work out "which index is
# closest to this colour" and for that it needs the three channels separately.
# Same roles as every other template: accent is primary, accent2 is tertiary.

accent  {{colors.primary.default.red}} {{colors.primary.default.green}} {{colors.primary.default.blue}}
accent2 {{colors.tertiary.default.red}} {{colors.tertiary.default.green}} {{colors.tertiary.default.blue}}
