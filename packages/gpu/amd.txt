# AMD, on the in-kernel amdgpu driver.
#
# NOT VERIFIED ON REAL HARDWARE. Every name here resolves against the
# configured repositories -- that much was checked -- but no AMD card has ever
# run this configuration, so treat the list as a starting point rather than as
# something that was seen to work. If you fix it, the fix belongs here.
#
# There is no kernel module to install: amdgpu is in the kernel already. What
# this list adds is the userspace half, and the lib32-* entries are the 32-bit
# copies Steam needs, which require [multilib] enabled.

lib32-mesa
lib32-vulkan-radeon
mesa
vulkan-radeon
